V4版本:
(实现了基本区块链条 + POW工作量证明 + Bolt数据存储 + 客户端操作)

上个版本:
1.定义了区块和区块链
2.实现了区块的创建(创世区块和普通区块)
3.实现了区块通过哈希指针串联成区块链
4.实现了哈希的生成算法
5.定义了工作量证明
6.将区块的Nonce变为可变,其结果为通过POW挖矿最终得到的Nonce
7.尝试各种Nonce,循环生成哈希,并与当前区块的Target比较
8.引入了Bolt数据库,增加了数据持久化功能
9.实现了将新增的block信息编码为字节流并存储入Bolt数据库的功能
10.实现了从Bolt数据库获取block信息并解码为结构体的功能
11.定义了迭代器,可以获取到所有区块的信息

更新:
12.定义了用户命令行输入工具,可以实现用户添加区块和查询区块
13.重构了main函数,现在可以自由添加区块的信息
(前几个版本只是开发者自嗨,而V4版本可以面向用户了)



